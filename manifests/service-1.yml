apiVersion: v1
kind: Service
metadata:
    name: "aimmopostcluster-d4f8"
    labels:
        app: "aimmopostcluster-d4f8"
spec:
    type: LoadBalancer
    ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
    selector:
        app: "aimmopostcluster-d4f8"