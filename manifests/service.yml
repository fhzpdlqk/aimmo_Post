apiVersion: v1
kind: Service
metadata:
    name: "aimmopostcluster-2a15"
    labels:
        app: "aimmopostcluster-2a15"
spec:
    type: LoadBalancer
    ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
    selector:
        app: "aimmopostcluster-2a15"