apiVersion: v1
kind: Service
metadata:
    name: "aimmopostcluster-2402"
    labels:
        app: "aimmopostcluster-2402"
spec:
    type: LoadBalancer
    ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
    selector:
        app: "aimmopostcluster-2402"