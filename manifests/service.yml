apiVersion: v1
kind: Service
metadata:
    name: "aimmopostcluster-b0f2"
    labels:
        app: "aimmopostcluster-b0f2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aimmopostcluster-b0f2"